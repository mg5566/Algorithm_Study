#include <iostream>

#include <string>
#include <vector>

using namespace std;

int solution(int N, int number) {
    int answer = 0;

    return answer;
}

int main(void) {
  cout << "solution: " << solution(5, 12) << endl;
  cout << "solution: " << solution(2, 11) << endl;
  cout << "solution: " << solution(5, 3600) << endl;
  return (0);
}
